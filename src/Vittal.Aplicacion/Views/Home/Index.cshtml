@{
    ViewData["Title"] = "Panel Principal";

    // Helper para verificar si el usuario puede ver un módulo
    bool PuedeVer(string clave)
    {
        if (ViewBag.EsAdmin == true) return true;
        var modulos = ViewBag.ModulosPermitidos as System.Collections.Generic.HashSet<string>;
        return modulos?.Contains(clave) == true;
    }

    // Contar módulos visibles por sección para decidir si mostrar el card
    var adminModulos = new[] { "perfiles", "usuarios", "permisos", "salas" };
    var catalogosModulos = new[] { "pacientes", "medicamentos", "clinicas", "tipos_cirugia", "cirugias",
                                   "tipos_dx", "diagnosticos", "tratamientos", "recomendaciones", "examenes" };
    bool hayAdmin = adminModulos.Any(PuedeVer);
    bool hayCatalogos = catalogosModulos.Any(PuedeVer);
}

@section HeaderActions {
    <small class="text-muted">
        <i class="bi bi-calendar3 me-1"></i>@DateTime.Now.ToString("dddd, dd 'de' MMMM 'de' yyyy", new System.Globalization.CultureInfo("es-ES"))
    </small>
}

<!-- Bienvenida -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card bg-primary text-white border-0 shadow-sm">
            <div class="card-body py-4">
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                        <div class="flex-shrink-0 me-3">
                            <i class="bi bi-heart-pulse" style="font-size: 2.5rem;"></i>
                        </div>
                        <div>
                            <h4 class="mb-1">Bienvenido al Sistema Vittal</h4>
                            <p class="mb-0 opacity-75">Sistema de gestion de citas y expedientes medicos</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modulos disponibles -->
<h5 class="mb-3">
    <i class="bi bi-grid-3x3-gap-fill me-2"></i>Modulos del Sistema
</h5>

<div class="row g-3 mb-4">
    @if (hayAdmin)
    {
    <!-- Administracion -->
    <div class="col-12">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-header bg-white border-bottom py-3">
                <h6 class="mb-0">
                    <i class="bi bi-gear-wide-connected text-primary me-2"></i>Administracion
                </h6>
            </div>
            <div class="card-body p-0">
                <div class="row g-0">
                    @if (PuedeVer("perfiles"))
                    {
                    <!-- Perfiles -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <a href="/Administracion/Perfil" class="module-card d-block p-3 text-decoration-none text-dark">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-primary bg-opacity-10 text-primary rounded-circle p-2 me-2">
                                    <i class="bi bi-person-badge"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Perfiles</div>
                                    <span class="badge bg-success" style="font-size: 0.65rem;">Activo</span>
                                </div>
                            </div>
                            <div class="text-muted small">Gestion de perfiles y roles del sistema</div>
                        </a>
                    </div>
                    }

                    @if (PuedeVer("usuarios"))
                    {
                    <!-- Usuarios -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <a href="/Administracion/Usuario" class="module-card d-block p-3 text-decoration-none text-dark">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-primary bg-opacity-10 text-primary rounded-circle p-2 me-2">
                                    <i class="bi bi-people"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Usuarios</div>
                                    <span class="badge bg-success" style="font-size: 0.65rem;">Activo</span>
                                </div>
                            </div>
                            <div class="text-muted small">Gestion de usuarios del sistema</div>
                        </a>
                    </div>
                    }

                    @if (PuedeVer("permisos"))
                    {
                    <!-- Permisos -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <a href="/Administracion/Permiso" class="module-card d-block p-3 text-decoration-none text-dark">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-primary bg-opacity-10 text-primary rounded-circle p-2 me-2">
                                    <i class="bi bi-key"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Permisos</div>
                                    <span class="badge bg-success" style="font-size: 0.65rem;">Activo</span>
                                </div>
                            </div>
                            <div class="text-muted small">Configuracion de permisos por modulo</div>
                        </a>
                    </div>
                    }

                    @if (PuedeVer("salas"))
                    {
                    <!-- Asignar Salas -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <a href="/Administracion/Sala" class="module-card d-block p-3 text-decoration-none text-dark">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-primary bg-opacity-10 text-primary rounded-circle p-2 me-2">
                                    <i class="bi bi-door-open"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Asignar Salas</div>
                                    <span class="badge bg-success" style="font-size: 0.65rem;">Activo</span>
                                </div>
                            </div>
                            <div class="text-muted small">Asignacion de salas y areas medicas</div>
                        </a>
                    </div>
                    }
                </div>
            </div>
        </div>
    </div>
    }
</div>

@if (hayCatalogos)
{
<div class="row g-3 mb-4">
    <!-- Catalogos -->
    <div class="col-12">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-header bg-white border-bottom py-3">
                <h6 class="mb-0">
                    <i class="bi bi-book text-success me-2"></i>Catalogos
                </h6>
            </div>
            <div class="card-body p-0">
                <div class="row g-0">
                    @if (PuedeVer("pacientes"))
                    {
                    <!-- Pacientes -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <a href="/Catalogos/Paciente" class="module-card d-block p-3 text-decoration-none text-dark">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-success bg-opacity-10 text-success rounded-circle p-2 me-2">
                                    <i class="bi bi-people"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Pacientes</div>
                                    <span class="badge bg-success" style="font-size: 0.65rem;">Activo</span>
                                </div>
                            </div>
                            <div class="text-muted small">Gestion de pacientes de la clinica</div>
                        </a>
                    </div>
                    }

                    @if (ViewBag.EsAdmin == true)
                    {
                    <!-- Medicamentos - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-capsule"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Medicamentos</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Catalogo de medicamentos</div>
                        </div>
                    </div>

                    <!-- Clinicas - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-building"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Clinicas</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Gestion de clinicas y sucursales</div>
                        </div>
                    </div>

                    <!-- Cirugias - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-scissors"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Cirugias</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Tipos de cirugias y procedimientos</div>
                        </div>
                    </div>

                    <!-- Diagnosticos - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-clipboard2-pulse"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Diagnosticos</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Tipos de diagnosticos y diagnosticos</div>
                        </div>
                    </div>

                    <!-- Tratamientos - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-heart-pulse"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Tratamientos</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Catalogo de tratamientos medicos</div>
                        </div>
                    </div>

                    <!-- Examenes - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-eyedropper"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Examenes</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Catalogo de examenes medicos</div>
                        </div>
                    </div>

                    <!-- Recomendaciones - Proximamente (solo admin) -->
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="module-card p-3 text-muted">
                            <div class="d-flex align-items-center mb-2">
                                <div class="module-icon bg-secondary bg-opacity-10 text-secondary rounded-circle p-2 me-2">
                                    <i class="bi bi-chat-square-text"></i>
                                </div>
                                <div>
                                    <div class="fw-semibold small">Recomendaciones</div>
                                    <span class="badge bg-warning text-dark" style="font-size: 0.65rem;">Proximamente</span>
                                </div>
                            </div>
                            <div class="small">Catalogo de recomendaciones medicas</div>
                        </div>
                    </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
}

@if (ViewBag.EsAdmin == true)
{
<!-- Proximamente: modulos avanzados (solo visible para admin) -->
<div class="row g-3 mt-1">
    <div class="col-12">
        <div class="card border-0 shadow-sm">
            <div class="card-body text-center py-4 text-muted">
                <i class="bi bi-cone-striped fs-3 d-block mb-2"></i>
                <p class="mb-0">Los modulos de Expedientes, Agenda, Cola de Espera, Dashboard y Reportes estaran disponibles proximamente.</p>
            </div>
        </div>
    </div>
</div>
}

@section Scripts {
<style>
    /* Module card hover effects */
    .module-card {
        transition: all 0.2s ease-in-out;
        border-radius: 0.5rem;
    }

    .module-card:hover {
        background-color: #f8f9fa;
        transform: translateY(-2px);
        box-shadow: 0 0.25rem 0.75rem rgba(0, 0, 0, 0.08);
    }

    .module-icon {
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.1rem;
    }

    /* Disabled module card */
    .text-muted .module-card {
        cursor: not-allowed;
        opacity: 0.7;
    }

    .text-muted .module-card:hover {
        transform: none;
        box-shadow: none;
        background-color: transparent;
    }

    /* Banner logo watermark */
    .banner-logo {
        max-width: 45px;
        height: auto;
        opacity: 0.15;
        filter: brightness(0) invert(1);
    }
</style>
}
